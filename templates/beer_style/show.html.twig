{% extends 'base.html.twig' %}

{% block page_title %}Beer Style{% endblock %}
{% block page_subtitle %}configuration{% endblock %}

{% block topRight %}
    <a href="{{ path('beer_style_index') }}" class="btn btn-default"><i class="fas fa-list"></i></a>
    <a href="{{ path('beer_style_edit', {'id': beer_style.id}) }}" class="btn btn-default"><i class="fas fa-edit"></i></a>
    {{ include('beer_style/_delete_form.html.twig') }}
{% endblock %}

{% block body %}
    <table class="table">
        <tbody>
            <tr>
                <th>Name</th>
                <td>{{ beer_style.name }}</td>
            </tr>
        </tbody>
    </table>
{% endblock %}
