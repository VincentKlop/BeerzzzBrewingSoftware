{% extends 'base.html.twig' %}

{% block page_title %}Unit Of Measurement Type{% endblock %}
{% block page_subtitle %}configuration{% endblock %}

{% block topRight %}
    <a href="{{ path('unit_of_measure_type_index') }}" class="btn btn-default"><i class="fas fa-list"></i></a>
    <a href="{{ path('unit_of_measure_type_edit', {'id': unit_of_measure_type.id}) }}" class="btn btn-default"><i class="fas fa-edit"></i></a>
    {{ include('unit_of_measure_type/_delete_form.html.twig') }}
{% endblock %}

{% block body %}
  <table class="table">
        <tbody>
            <tr>
                <th>Name</th>
                <td>{{ unit_of_measure_type.name }}</td>
            </tr>
        </tbody>
    </table>
{% endblock %}
