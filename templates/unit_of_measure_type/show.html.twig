{% extends 'base.html.twig' %}

{% block page_title %}Unit Of Measurement Type{% endblock %}
{% block page_subtitle %}configuration{% endblock %}

{% block page_content %}
  <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ unit_of_measure_type.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ unit_of_measure_type.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('unit_of_measure_type_index') }}"><i class="fas fa-list"></i></a>

    <a href="{{ path('unit_of_measure_type_edit', {'id': unit_of_measure_type.id}) }}"><i class="fas fa-edit"></i></a>

    {{ include('unit_of_measure_type/_delete_form.html.twig') }}
{% endblock %}
