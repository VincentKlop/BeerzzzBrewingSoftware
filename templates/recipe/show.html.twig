{% extends 'base.html.twig' %}

{% block page_title %}Recipe{% endblock %}
{% block page_subtitle %}{% endblock %}

{% block topRight %}
    <a href="{{ path('recipe_pdf', {'id': recipe.id }) }}" class="btn btn-default"><i class="fas fa-file-pdf"></i></a>
    <a href="{{ path('recipe_index') }}" class="btn btn-default"><i class="fas fa-list"></i></a>
    <a href="{{ path('recipe_edit', {'id': recipe.id}) }}" class="btn btn-default"><i class="fas fa-edit"></i></a>
    {{ include('recipe/_delete_form.html.twig') }}
{% endblock %}

{% block body %}
    {{ include('recipe/recipeContent.html.twig') }}
{% endblock %}
