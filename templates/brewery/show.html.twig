{% extends 'base.html.twig' %}

{% block page_title %}Brewery{% endblock %}
{% block page_subtitle %}configuration{% endblock %}

{% block topRight %}
    <a href="{{ path('brewery_index') }}" class="btn btn-default"><i class="fas fa-list"></i></a>
    <a href="{{ path('brewery_edit', {'id': brewery.id}) }}" class="btn btn-default"><i class="fas fa-edit"></i></a>
    {{ include('brewery/_delete_form.html.twig') }}
{% endblock %}

{% block body %}
    <table class="table">
        <tbody>
            <tr>
                <td>{{ brewery.id }}</td>
            </tr>
            <tr>
                <td>{{ brewery.name }}</td>
            </tr>
        </tbody>
    </table>
{% endblock %}
